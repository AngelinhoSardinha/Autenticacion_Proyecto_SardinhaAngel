<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Proyecto en progreso</title>
    <link rel="stylesheet" type="text/css" href="styles.css">
</head>

<body>
    <header>
        <h1>Gestió de Reservas per <%= user.name %>!</h1> 
        <h2><%- htmlMessage %></h2>
    </header>
    <main>
        <section>
            <h2>Llista de Reservas</h2>
            <ul class="product-list">
                <% data.reserves.forEach(reserves=> { %>
                    <li class="product-item">
                        <strong>
                            <%= reserves.name %>
                        </strong>
                        <span>nombre: <%= reserves.Estat %></span>
                        <span>nombre: <%= reserves.Temps %></span>
                        <span><a href="/reserves/<%=reserves.id%>" class="btn-formulario">Detalle de la reserva</a>
                        <a href="reservesForm.html" class="btn-formulario">Modificar Recurso</a></span>
                    </li>
                    <% }) %>
            </ul>
        </section>
    </main>
</body>

</html>